camilla_host: "127.0.0.1"
camilla_port: 1234
bind_address: "0.0.0.0"
port: 5005
# The camillagui backend apparently doesn't understand environment variable references.
# Use paths relative to the currently working directory (music_server) instead.
# These need to match those in env.sh
config_dir: camilladsp/configs
coeff_dir: camilladsp/coeffs
default_config: "default_config.yml"
statefile_path: camilladsp/statefile.yml
log_file: logs/camilladsp.log"
